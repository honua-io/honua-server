extensions:
  sigv4auth:
    service: "aps"
    region: "${AWS_REGION}"

exporters:
  prometheusremotewrite:
    endpoint: "${PROM_RW_ENDPOINT}"
    auth:
      authenticator: sigv4auth

service:
  extensions: [sigv4auth]
