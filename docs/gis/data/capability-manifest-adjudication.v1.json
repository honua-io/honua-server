{
  "schemaVersion": "1.0.0",
  "trackingIssue": "https://github.com/honua-io/honua-server/issues/3607",
  "description": "The hand-written half of the capability-manifest crosswalk (WS1, ADR-0058). Every honua.capability_manifest.v1 id that does not already resolve — by matching a capability key directly, or through its EntitlementKey — is adjudicated here exactly once. The mechanical half is derived by scripts/ci/generate-capability-manifest-crosswalk.py; nothing in this file may restate what that script can compute.",
  "dispositions": {
    "mapped": "The manifest id is the wire name for an existing licensed capability. `capability` names it.",
    "register": "A real capability the licensing registry does not model yet. `capability` names the key to add; until it exists the crosswalk reports the id as unresolved and the gate fails, which is the point.",
    "not-licensable": "The manifest advertises something that is not a separately licensable capability — a baseline every protocol already includes, an umbrella over a family that is licensed per-operation, a schema-version advertisement, or a client-interop fact tracked by another crosswalk."
  },
  "adjudications": [
    {
      "manifestId": "jobs.runner",
      "disposition": "register",
      "capability": "jobs.durable-runtime",
      "note": "First, per #3607. The durable job substrate behind imports, tile operations, geoprocessing and workflows. It resolves in no published catalog while CapabilityUnavailableCodes.DurableJobsCapability names it and live typed refusals hand it to clients, so an agent receiving the refusal cannot look the id up. process.geoprocessing is the nearest existing key and is wrong: the substrate is broader than geoprocessing and is what fails when Redis is absent."
    },
    {
      "manifestId": "transport.grpc",
      "disposition": "register",
      "capability": "serve.grpc",
      "note": "gRPC is a documented protocol in the protocol matrix, served on port 8081, with SDK and mobile clients depending on it — and it is the only served protocol with no serve.* key. The other 24 all have one. One key covers the three wire variants below; they are transports of the same surface, not separate capabilities."
    },
    {
      "manifestId": "transport.grpc-web",
      "disposition": "register",
      "capability": "serve.grpc",
      "note": "gRPC-Web on port 8080. Same capability as transport.grpc, different wire framing for browsers."
    },
    {
      "manifestId": "transport.native-grpc",
      "disposition": "register",
      "capability": "serve.grpc",
      "note": "Native h2c gRPC on port 8081. Same capability as transport.grpc."
    },
    {
      "manifestId": "transport.mcp",
      "disposition": "mapped",
      "capability": "ai.mcp-discovery",
      "note": "The /mcp surface is licensed through the AI family, where MCP discovery and query are Community and agent operations are Pro. The transport id is the manifest's name for the same surface."
    },
    {
      "manifestId": "transport.qgis",
      "disposition": "not-licensable",
      "capability": null,
      "note": "QGIS is a client, not a protocol Honua serves. It reaches the server over OGC API Features, WFS and WMTS, each already licensed by its own serve.* key. Client support is tracked by the interop crosswalk and the client-certification matrix, not by a licensing key."
    },
    {
      "manifestId": "query.features",
      "disposition": "not-licensable",
      "capability": null,
      "note": "Protocol-neutral feature query is the baseline every serve.* capability already includes; FeatureServer query, OGC API Features items and WFS GetFeature are the same read through different wires. Mapping it to one protocol would misattribute the others, and mapping it to all 24 would double-count."
    },
    {
      "manifestId": "analysis.spatial",
      "disposition": "not-licensable",
      "capability": null,
      "note": "An umbrella the manifest advertises so a client can ask whether spatial analysis exists at all. Licensing is per operation — analytics.viewshed, analytics.h3, analytics.spatial-join and the rest are individually keyed and individually Pro. A single umbrella key would let one entitlement stand in for eleven."
    },
    {
      "manifestId": "package.metadata-v2",
      "disposition": "not-licensable",
      "capability": null,
      "note": "A package-family schema-version advertisement. The manifest publishes which package schema versions a deployment understands so a client can negotiate; the capability being exercised is the admin control plane that accepts the package."
    },
    {
      "manifestId": "package.release-package",
      "disposition": "not-licensable",
      "capability": null,
      "note": "Schema-version advertisement, as package.metadata-v2."
    },
    {
      "manifestId": "package.gitops-manifest",
      "disposition": "not-licensable",
      "capability": null,
      "note": "Schema-version advertisement, as package.metadata-v2."
    },
    {
      "manifestId": "package.map",
      "disposition": "not-licensable",
      "capability": null,
      "note": "Schema-version advertisement. Its policyCapability is studio.edit, which is an authorization policy name rather than a licensing key."
    },
    {
      "manifestId": "package.app",
      "disposition": "not-licensable",
      "capability": null,
      "note": "Schema-version advertisement, as package.map."
    },
    {
      "manifestId": "gitops.release-manifest",
      "disposition": "mapped",
      "capability": "admin.control-plane",
      "note": "Declarative catalog apply runs through the admin control plane, which is the key covering administrative CRUD with no dedicated surface of its own."
    },
    {
      "manifestId": "publication.metadata-release",
      "disposition": "mapped",
      "capability": "admin.control-plane",
      "note": "Backed by a real route (MetadataReleaseOperationEndpoints) gated on the manifest id, with catalog.publish as its authorization policy. The licensed capability is the admin control plane the route belongs to."
    },
    {
      "manifestId": "collaboration.feature-locks",
      "disposition": "mapped",
      "capability": "collaboration.map-sessions",
      "note": "Feature locks are the concurrency mechanism inside a collaboration session; they exist to make concurrent editing in a shared map safe. One licensed capability, two names."
    },
    {
      "manifestId": "collaboration.feature-locks.cross-node",
      "disposition": "mapped",
      "capability": "collaboration.map-sessions",
      "note": "Cross-node lock coordination is a deployment property of the same capability — it is what the locks require once the server runs multi-node — not a capability a customer licenses separately."
    },
    {
      "manifestId": "edit.geoservices-version-tokens",
      "disposition": "mapped",
      "capability": "editing.branch-versioning",
      "note": "Version tokens are the GeoServices wire form of branch versioning; the same entitlement gates both."
    },
    {
      "manifestId": "operate.status",
      "disposition": "mapped",
      "capability": "ops.health",
      "note": "The authenticated operate-status surface reports the same health the ops.health capability covers."
    }
  ]
}
