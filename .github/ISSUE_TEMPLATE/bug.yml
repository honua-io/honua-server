name: Bug Report
description: Report a bug or regression with reproducible details
title: "bug: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting this. Please include enough detail for us to reproduce and fix the issue quickly.

  - type: textarea
    id: summary
    attributes:
      label: What happened?
      description: A concise summary of the bug.
    validations:
      required: true

  - type: textarea
    id: screenshots
    attributes:
      label: Screenshots or recordings
      description: Drag and drop screenshots, GIFs, or screen recordings here (or paste links).
    validations:
      required: true

  - type: textarea
    id: reproduction
    attributes:
      label: Steps to Reproduce
      description: How can we reproduce this issue?
      placeholder: |
        1. Go to ...
        2. Run ...
        3. Observe ...
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected Behavior
      description: What should happen?
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Actual Behavior
      description: What actually happens?
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Provide runtime and client details so we can reproduce accurately.
      placeholder: |
        - Honua version/commit:
        - Deployment: (Docker, local, cloud)
        - OS:
        - Browser/client + version:
        - Database: (PostgreSQL/PostGIS version)
        - Any relevant config:
    validations:
      required: true
