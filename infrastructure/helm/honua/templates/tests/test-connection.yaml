apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "honua.fullname" . }}-test-connection"
  labels:
    {{- include "honua.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:8.5.0
      command:
        - sh
        - -c
        - |
          set -e
          url="http://{{ include "honua.fullname" . }}:{{ .Values.service.port }}/healthz/ready"
          host_header="{{ (index .Values.ingress.hosts 0).host }}"
          attempts=30
          sleep_seconds=5
          i=1
          while [ "$i" -le "$attempts" ]; do
            if curl -fsS --max-time 10 -H "Host: ${host_header}" "$url" >/dev/null; then
              exit 0
            fi
            echo "[$i/$attempts] waiting for readiness at $url (Host: ${host_header})"
            i=$((i + 1))
            sleep "$sleep_seconds"
          done
          echo "readiness check failed after $attempts attempts: $url"
          exit 1
