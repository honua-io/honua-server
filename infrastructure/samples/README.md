# Infrastructure Samples

This directory contains infrastructure-as-code (IaC) samples for Honua Server.

- docker-compose/ - Local Docker Compose stack (PostGIS + Redis + Honua Server; optional pgAdmin).
