{
  "schemaVersion": 1,
  "description": "Pages whose subject is a wire protocol and for which raw HTTP examples are unavoidable. Every curl example on an allowlisted page must be marked <!-- wire-reference -->, and the exception must be justified in the PR body.",
  "entries": [
    {
      "path": "docs/user/namespaced-layer-publication.md",
      "reason": "Wire contract reference for the additive namespace request field before released CLI/SDK surfaces expose it; the marked HTTP example supports client implementers and interoperability fixtures, not a replacement operator workflow."
    }
  ]
}
