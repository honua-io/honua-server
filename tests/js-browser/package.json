{
  "name": "honua-maplibre-browser-tests",
  "version": "1.0.0",
  "description": "MapLibre GL JS browser compatibility suite for Honua Server",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "~1.50.0",
    "@types/node": "^20.10.0",
    "maplibre-gl": "~5.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
