{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://geospatial-mcp.honua.io/spec/schemas/tools/list_capabilities.schema.json",
  "title": "list_capabilities inputSchema",
  "description": "Argument shape for bounded capability discovery. Full inventory export is an explicit admin-only operation.",
  "x-honua-extension": true,
  "x-honua-reference-shape": true,
  "type": "object",
  "properties": {
    "includeResources": {
      "type": "boolean",
      "default": true,
      "description": "Include the resource catalog (job, catalog, and promotion URIs) in the manifest."
    },
    "includeGroundingResources": {
      "type": "boolean",
      "default": true,
      "description": "Include the grounding/catalog resource URIs a client LLM should read first to ground a workflow."
    },
    "toolCursor": {
      "type": "string",
      "description": "Opaque cursor returned as nextToolCursor."
    },
    "resourceCursor": {
      "type": "string",
      "description": "Opaque cursor returned as nextResourceCursor."
    },
    "fullExport": {
      "type": "boolean",
      "default": false,
      "description": "Explicit admin-only full inventory export."
    }
  }
}
