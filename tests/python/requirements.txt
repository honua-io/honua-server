# Honua Integration Test Suite Dependencies
# Python >= 3.11 required

# Core testing framework
pytest>=8.0.0
pytest-xdist>=3.5.0  # Parallel test execution
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0

# HTTP client
httpx>=0.27.0
requests>=2.31.0
pystac>=1.10.0
pystac-client>=0.8.0
stac-api-validator==0.6.8

# Database and containers
testcontainers[postgres]>=4.0.0
psycopg[binary]>=3.1.0

# Geospatial validation
shapely>=2.0.0
geopandas>=0.14.0
pyproj>=3.6.0

# Optional: PyQGIS integration (for desktop client compatibility tests)
# Note: PyQGIS is a system dependency provided by the QGIS installation.
# It is NOT installable via pip. The pyqgis test suite auto-skips when
# qgis.core is not importable. See docs/contributor/testing-python.md.

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
