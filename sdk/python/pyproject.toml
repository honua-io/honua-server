[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "honua-sdk"
version = "0.0.1a0"
description = "Python SDK scaffold for Honua Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
